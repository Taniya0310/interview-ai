CREATE TABLE IF NOT EXISTS answers(id uuid PRIMARY KEY DEFAULT gen_random_uuid(), interview_id uuid NOT NULL REFERENCES interviews(id) ON DELETE CASCADE, interview_question_id uuid NOT NULL REFERENCES interview_questions(id), video_path text NOT NULL, mime_type text NOT NULL DEFAULT 'video/webm', status text NOT NULL DEFAULT 'pending', error_message text, created_at timestamptz NOT NULL DEFAULT now());
